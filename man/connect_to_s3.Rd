% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect_to_s3.R
\name{connect_to_s3}
\alias{connect_to_s3}
\title{Open a DuckDB connection configured for S3 access}
\usage{
connect_to_s3(
  bucket,
  region = "us-east-1",
  vending_url = default_vending_url(),
  require_local = FALSE,
  dbdir = ":memory:"
)
}
\arguments{
\item{bucket}{Character. S3 bucket to request vended credentials for.
Required when falling back to the vending endpoint.}

\item{region}{Character. AWS region for the S3 secret. Default: \code{"us-east-1"}.}

\item{vending_url}{Character. URL of the credential-vending endpoint. Only
used when no local AWS credentials are configured. Defaults to the
\code{"cori.data.vending_url"} option, then the \code{CORI_DATA_VENDING_URL}
environment variable, then the deployed CORI endpoint.}

\item{require_local}{Logical. If \code{TRUE}, error immediately when no local
AWS credentials are configured instead of falling back to (read-only)
vended credentials. Use this for connections that will write to S3.
Default: \code{FALSE}.}

\item{dbdir}{Character. Path to a persistent DuckDB database file. Default
\code{":memory:"} opens an in-memory instance, matching prior behavior. Pass
a file path for callers that need the catalog/temp state to persist
across a script run.}
}
\value{
An open \code{duckdb_connection}. The caller owns the connection and
must disconnect it, e.g. \code{on.exit(DBI::dbDisconnect(con, shutdown = TRUE))}.
}
\description{
Creates a DuckDB connection with the \code{httpfs} and \code{aws} extensions loaded
and an S3 secret configured. This is the shared connection utility for
\verb{cori.data.*} packages, so S3/DuckDB setup lives in one place instead of
being duplicated in each package's \verb{read_*_from_s3()} function.
}
\details{
Credentials are resolved in two ways, in order:
\enumerate{
\item If \code{\link[=has_local_aws_credentials]{has_local_aws_credentials()}} finds credentials in the environment
or \verb{~/.aws/credentials}, the connection uses the caller's own identity
via \verb{PROVIDER CREDENTIAL_CHAIN} — no network round-trip.
\item Otherwise, short-lived read-only credentials for \code{bucket} are fetched
from \code{vending_url} and installed directly as a static secret.
}

\code{bucket} is only used for the vending path, where the endpoint requires
it; it is ignored when local credentials are present. Vended credentials
are read-only (across the allowlisted \verb{cori.data.*} buckets), so writes
through this connection require local credentials -- pass
\code{require_local = TRUE} to fail fast with a clear message instead of
getting an opaque S3 access-denied error mid-query.
}
\examples{
\dontrun{
  con <- connect_to_s3("cori.data.bds")
  on.exit(DBI::dbDisconnect(con, shutdown = TRUE))
  DBI::dbGetQuery(con, "SELECT * FROM read_parquet('s3://cori.data.bds/**/*.parquet')")
}

}

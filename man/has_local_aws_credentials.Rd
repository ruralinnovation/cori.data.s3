% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect_to_s3.R
\name{has_local_aws_credentials}
\alias{has_local_aws_credentials}
\title{Detect locally configured AWS credentials}
\usage{
has_local_aws_credentials()
}
\value{
Logical. \code{TRUE} if credentials are present in either standard
location.
}
\description{
Checks the two places DuckDB's own \verb{CHAIN 'env;config'} would look for
credentials: the \code{AWS_ACCESS_KEY_ID} / \code{AWS_SECRET_ACCESS_KEY} environment
variables, and \verb{~/.aws/credentials}. Keeping the detection aligned with
the chain DuckDB actually uses means "already has credentials" means the
same thing to both.
}
\details{
This deliberately does not detect credentials sourced from an EC2 instance
profile, an ECS task role, or an SSO session that only populated a cached
token (\verb{~/.aws/sso/}). Callers in those environments fall through to the
vending path even though they could authenticate another way — the
conservative choice, since the vending path is the tracked, read-only one.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync_s3_to_local.R
\name{sync_s3_to_local}
\alias{sync_s3_to_local}
\title{Sync S3 objects to a local directory}
\usage{
sync_s3_to_local(
  bucket,
  prefix,
  local_path,
  region = "us-east-1",
  vending_url = default_vending_url(),
  extra_args = character(0)
)
}
\arguments{
\item{bucket}{Character. S3 bucket name (without \verb{s3://} prefix).}

\item{prefix}{Character. S3 key prefix within the bucket (e.g.,
\code{"nbm_block-D25/state_abbr=NC/"}). Include trailing slash for directories.}

\item{local_path}{Character. Local directory path to sync to. Created if it
does not exist.}

\item{region}{Character. AWS region for the bucket. Default: \code{"us-east-1"}.}

\item{vending_url}{Character. URL of the credential-vending endpoint. Only
used when no local AWS credentials are configured. Defaults to the
\code{"cori.data.vending_url"} option, then the \code{CORI_DATA_VENDING_URL}
environment variable, then the deployed CORI endpoint.}

\item{extra_args}{Character vector. Additional arguments to pass to
\verb{aws s3 sync} (e.g., \code{c("--delete", "--quiet")}). Default: \code{character(0)}.}
}
\value{
Invisibly returns the exit status of the \verb{aws s3 sync} command
(0 on success). Throws an error if the sync fails.
}
\description{
Downloads objects from an S3 prefix to a local directory using \verb{aws s3 sync}.
Uses the same credential resolution as \code{\link[=connect_to_s3]{connect_to_s3()}}: local credentials
if available, otherwise short-lived vended credentials from the CORI
credential-vending endpoint.
}
\details{
When vended credentials are used, they are temporarily injected as
environment variables (\code{AWS_ACCESS_KEY_ID}, \code{AWS_SECRET_ACCESS_KEY},
\code{AWS_SESSION_TOKEN}) for the duration of the sync, then restored.

Requires the AWS CLI (\code{aws}) to be installed and on the PATH.
}
\examples{
\dontrun{
  sync_s3_to_local(
    bucket = "cori.data.fcc",
    prefix = "nbm_block-D25/state_abbr=NC/",
    local_path = file.path(tempdir(), "nbm_block-D25", "state_abbr=NC")
  )
}

}
